# Copy to .env and fill values
PORT=4000
ADMIN_TOKEN=change-me-to-a-secure-token
RECIPIENT_EMAIL=parshuram8792@gmail.com
# SMTP settings (for Nodemailer). If you don't have an SMTP server, use SendGrid SMTP or Gmail SMTP (not recommended for production).
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-smtp-username
SMTP_PASS=your-smtp-password
FROM_EMAIL=your-from@example.com
# Optional: when set, backend will forward submissions to this Apps Script URL
APPS_SCRIPT_URL=
